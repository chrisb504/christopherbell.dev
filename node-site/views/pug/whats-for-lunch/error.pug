extends layout

block content
  #container
        #nav
        #main
            div(class='jumbotron')
                div(class='container')
                    h1(class='text-center')=title
            #body
                #body-title
                    h1=message
                    h3=error.status
                #content
                    pre #{error.stack}
            footer(id='footer')
